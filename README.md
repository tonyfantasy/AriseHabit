# Habit Tracker

Трекер привычек с графиком прогресса и напоминаниями. Можно пользоваться локально или выложить в интернет и делиться ссылкой с друзьями.

## Как сделать сайт общедоступным (поделиться с друзьями)

Бесплатный вариант — **GitHub Pages**. После этого у вас будет ссылка вида `https://ВАШ_ЛОГИН.github.io/habit-tracker/`, которой можно поделиться.

### Шаги

1. **Создайте аккаунт на GitHub** (если ещё нет): [github.com](https://github.com).

2. **Создайте новый репозиторий:**
   - Нажмите **New repository**.
   - Имя, например: `habit-tracker`.
   - Public, без галочки «Add a README» — нажмите **Create repository**.

3. **Загрузите файлы приложения** (из папки `habit app`):
   - В корень репозитория должны попасть: `index.html`, `styles.css`, `app.js` (и при желании этот `README.md`).
   - Через веб-интерфейс: **Add file → Upload files** — перетащите эти файлы, затем **Commit changes**.

4. **Включите GitHub Pages:**
   - В репозитории: **Settings** → слева **Pages**.
   - В блоке **Source** выберите **Deploy from a branch**.
   - **Branch:** `main` (или `master`), папка **/ (root)** → **Save**.

5. **Подождите 1–2 минуты.** Ссылка на сайт появится в том же разделе Pages:  
   `https://ВАШ_ЛОГИН.github.io/habit-tracker/`

Этой ссылкой можно делиться. У каждого, кто откроет её, свои данные (они хранятся в браузере на его устройстве). Уведомления работают по HTTPS.

### Получение отзывов в Telegram (@antnndrv)

Внизу страницы есть блок **«Обратная связь»**: отзывы отправляются в Telegram. Чтобы они приходили вам:

1. **Создайте бота** в Telegram: напишите [@BotFather](https://t.me/BotFather), команда `/newbot`, придумайте имя — он пришлёт **токен** (например `123456789:AAH...`).
2. **Узнайте свой chat_id** (числовой id вашего аккаунта). Варианты: напишите боту [@userinfobot](https://t.me/userinfobot) — он пришлёт ваш Id; или напишите своему новому боту любое сообщение, затем откройте в браузере `https://api.telegram.org/bot<ВАШ_ТОКЕН>/getUpdates` — в ответе в `message.chat.id` будет ваш chat_id.
3. В `index.html` найдите секцию с `id="feedbackSection"` и заполните атрибуты:
   - `data-feedback-bot-token="СЮДА_ВСТАВЬТЕ_ТОКЕН_БОТА"`
   - `data-feedback-chat-id="СЮДА_ВСТАВЬТЕ_ВАШ_CHAT_ID"` (только цифры).

После этого кнопка «Отправить в Telegram» будет отправлять отзывы вам в личку. **Важно:** токен бота будет виден в коде страницы; для личного использования это допустимо. При утечке токена создайте нового бота у BotFather и замените токен.

---

## Возможности

- **Привычки** — добавление, редактирование, удаление. Для каждой привычки можно задать цель (сколько раз в день) и время напоминания.
- **Отметки** — клик по кружку отмечает выполнение (можно снимать отметку). Поддерживается счётчик: например, «3 стакана воды» = цель 3, отмечаете до 3 раз.
- **График** — за последние 7 дней показывается процент выполнения по каждой привычке (линейный график).
- **Уведомления** — кнопка «🔔» в шапке запрашивает разрешение и включает напоминания. В заданное время приходит системное уведомление, если привычка ещё не выполнена. Напоминание показывается не чаще одного раза в день на привычку.

## Запуск

Откройте `index.html` в браузере (двойной клик или через локальный сервер).

Для уведомлений нужен HTTPS или `localhost`. При открытии через `file://` уведомления могут не работать — используйте простой локальный сервер:

```bash
# Python 3
python3 -m http.server 8000

# или npx
npx serve .
```

Затем откройте в браузере `http://localhost:8000`.

Данные хранятся в `localStorage` в том же браузере.
